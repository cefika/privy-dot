{
  "name": "privy-dot-contracts",
  "version": "0.2.0",
  "description": "ECPDKSAP PolkaVM contract — pvm-contract-macros",
  "engines": {
    "node": ">=22 <23",
    "npm": ">=10"
  },
  "scripts": {
    "deploy:local": "npx hardhat run scripts/deploy.ts --network local",
    "deploy:testnet": "npx hardhat run scripts/deploy.ts --network polkadotTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-viem": "^2.0.0",
    "hardhat": "^2.27.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2",
    "viem": "^2.0.0"
  }
}